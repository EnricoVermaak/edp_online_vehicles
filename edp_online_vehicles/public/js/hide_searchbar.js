// frappe.ready(function () {
// 	// Grab the list of roles the user has
// 	var roles = frappe.boot.user.roles || [];

// 	// Define which roles should NOT see the search bar
// 	var blocked = ["Dealer Vehicle Administrator"];

// 	// If the user has any of those roles, hide the search bar
// 	for (var i = 0; i < blocked.length; i++) {
// 		if (roles.indexOf(blocked[i]) !== -1) {
// 			$(".navbar-search, .toolbar-search").hide();
// 			break;
// 		}
// 	}
// });
