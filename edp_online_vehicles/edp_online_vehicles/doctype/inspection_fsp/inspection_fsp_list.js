frappe.listview_settings["Inspection FSP"] = {
	hide_name_column: true,
	hide_name_filter: true, // hide the default filter field for the name column
	hide_status_column: true,

	add_fields: ["inspection_status"],
};
